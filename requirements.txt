# ============================================================
#  Tax Authority Enterprise RAG — Requirements
# ============================================================
# Install:  pip install -r requirements.txt
# ============================================================

# --- LangGraph (CRAG state machine) ---
langgraph>=0.2.0

# --- LlamaIndex (hierarchical chunking + vector store) ---
llama-index>=0.11.0
llama-index-vector-stores-qdrant>=0.3.0
llama-index-embeddings-gemini>=0.2.0

# --- Vector DB ---
qdrant-client>=1.12.0

# --- Semantic Cache (Redis + RediSearch) ---
redis>=5.0.0

# --- Search & Reranking ---
sentence-transformers>=3.0.0
rank-bm25>=0.2.2

# --- LLM Providers (Grader + Generator) ---
langchain-google-genai>=2.0.0
langchain-anthropic>=0.3.0

# --- Utilities ---
python-dotenv>=1.0.0
numpy>=1.26.0
nltk>=3.9.0
